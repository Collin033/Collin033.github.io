---
title: "2022第一次月赛题解"
date: 2022-11-20 00:02:53
updated: 2023-09-21 23:11:15
tags:
  - 算法
categories:
  - 学习笔记
toc: true
legacy_source: "2022/11/20/2022第一次月赛题解/index.html"
---
# 2022第一次月赛题解

<!-- more -->

## 7-1 真假签到题

暴力搜索区间(l,r)内二进制1最多的数字即可

## 7-2 去徒步啦

正难反易
因为正向解决这个问题比较困难，所以我们可以将问题转化为求出不存在相邻元素颜色相同的染色方案数，然后用所有排列的情况减去。

对于序列中的每一个人，只要和前一个人的颜色不同就可以了，所以共有m-1种选择。对于第一个人无论选什么颜色都可以，那么就有m种选择。

所以ans=$m^n$-m*${(m-1)}^{(n-1)}$。

## 7-3 有趣的智能锁

直接设x=k1 * y+a, y=k2 * z+b, z=k3 * x+c，然后联立方程令k3=0，k1=k2=1即可解得x=a+b+c,y=b+c,z=c

## 7-4 通往塔尔之交要塞的道路

本题是二分法和最短路径算法的简单结合。对点权fi进行二分，用Dijkstra算法求最短路径，检验总边权是否小于b。

1. 对点权二分。题目的要求是：从1到N有很多路径，其中一条可行路径Pi，它有一个点的点权最大，记为Fi；在所有可行路径中，找到那条有最小F的路径，输出F。解题方案是先对所有的点fi排序，然后用二分法，找到符合要求的最小fi。二分次数$log_2$(fi)=$log_2$($10^9$)<30。
2. 在检查某个$f_i$时，删除所有大于$f_i$的点，在剩下的点中，求1到N的最短路径，看总边权是否小于b,如果满足，这个$f_i$是合适的（如果最短路的边权都大于b，那么其他路径的总边权就更大，肯定不符合要求）。一次Dijkstra求最短路的复杂度为O(m$log_2$n).

## 7-5 传送锚点

我们假设河流是墙

第一步：找到起点可以到达的点，放入vec1中。(不可穿墙)
第二步：找到终点可以到达的点，放入vec2中。
第三步：将vec1和vec2的点相互计算，算出最小的消耗

拿样例来说
起点是(4,5)，那么从起点可以到达的点有(5,4),(5,3),(5,5),(4,5),(3,5),(3,4)
终点是(1,1)，从终点可以到达的点只有(1,1)
这个时候 将起点可以到达的点和终点可以到达的点计算，可以看到(1,1)->(3,4)是最小消耗的

涉及知识：dfs搜索可以到达的点

## 7-6 DDL啦

本质上是计算二项分布，首先计算一次到站下车的期望，然后由于是多次重复,所以乘上站点数
即为m*(1-${((m-1)/m)}^n$);

## 7-7 天桐和幸运数字

很明显，当$a_i$ >= n * 10时，一定是天桐数
当$a_i$ <= n * 10时，考虑个位加法即可
